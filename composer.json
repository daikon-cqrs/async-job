{
    "name": "daikon/async-job",
    "type": "library",
    "description": "Asynchronous job components for Daikon projects.",
    "keywords": [ "asynchronous", "background", "delayed", "job", "worker" ],
    "homepage": "https://github.com/daikon-cqrs/async-job",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": "^7.2",
        "beberlei/assert": "^v3.0",
        "daikon/data-structure": "dev-upgrade/nativecalls",
        "daikon/message-bus": "dev-upgrade/nativecalls"
    },
    "require-dev": {
        "phpunit/phpunit": "^7.4",
        "sebastian/phpcpd": "^4.1",
        "squizlabs/php_codesniffer": "^3.3",
        "vimeo/psalm": "^2.0"
    },
    "autoload": {
        "psr-4": { "Daikon\\AsyncJob\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Daikon\\Tests\\AsyncJob\\": "tests" }
    },
    "scripts": {
        "build": [ "@sniff", "@tests" ],
        "code-sniffer-autofix": "phpcbf --standard=PSR2 -d tabWidth=4 src/",
        "code-sniffer": "phpcs -p -s --extensions=php --standard='PSR1,PSR2' src/",
        "copy-paste-detector": "phpcpd src/ || true",
        "sniff": [
            "@code-sniffer",
            "@copy-paste-detector",
            "@validate-json"
        ],
        "test": "phpunit --no-coverage",
        "tests": "phpunit",
        "validate-deps": "composer validate --with-dependencies --strict",
        "validate-json": "composer validate --no-check-all --no-check-lock"
    }
}
